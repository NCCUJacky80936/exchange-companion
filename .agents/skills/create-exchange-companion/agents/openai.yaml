interface:
  display_name: "建立交換手帳"
  short_description: "初始建立或依明確要求調整交換網站"
  default_prompt: "Use $create-exchange-companion for first setup or an explicitly requested exchange-site change, loading only the relevant reference and following AGENTS.md for deployment routing."
