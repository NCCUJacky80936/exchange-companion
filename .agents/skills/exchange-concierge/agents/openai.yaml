interface:
  display_name: "Exchange Concierge"
  short_description: "整理授權資料與官方證據，產生可審核的交換手帳更新提案"
  default_prompt: "Use $exchange-concierge. Pull the latest versioned website handoff when available, or use the exact latest handoff I explicitly provide. Reconcile affected surfaces and dependencies for a targeted run, or all surfaces only for a weekly, first-import, or explicitly comprehensive run. Validate schema and evidence coverage, then submit or deliver pending review proposals without applying them."
