interface:
  display_name: "Exchange Concierge"
  short_description: "整理授權資料與官方證據，產生可審核的交換手帳更新提案"
  default_prompt: "Use $exchange-concierge. If my private cloud connection exists, pull the latest versioned website handoff first; otherwise use the latest handoff JSON I explicitly provide. Within my authorized files and mailbox scope, reconcile every editable surface, validate schema and evidence coverage, then submit or deliver pending review proposals without applying them."
